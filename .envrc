# direnv: activate the virtualenv the functional suite runs in.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   direnv allow
#
# Without direnv installed this file does nothing; activate by hand instead.
if [ -d .venv ]; then
  source .venv/bin/activate
fi
