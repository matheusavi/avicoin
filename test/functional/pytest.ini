[pytest]
testpaths = .
pythonpath = .
markers =
    scenario: several real nodes, minutes rather than seconds. CI runs these;
        `-m "not scenario"` is the fast subset a developer wants mid-change.
